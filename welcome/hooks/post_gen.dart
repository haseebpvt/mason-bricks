import 'package:mason/mason.dart';

void run(HookContext context) {
  // List<String> arr = json.decode(context.vars['name']) as List<String>;

  // for(var i = 0; i < arr.length; i++) {
  //   print(arr[i]);
  // }
}
